apiVersion: v1
kind: Service
metadata:
  name: producer-service
  namespace: production
  labels:
    app: producer-service
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081
      protocol: TCP
      name: http
  selector:
    app: producer-service